function argestNumber(x,y){
    if(x>y){
        document.write("Output: the two numbers are:x and y, and the largest number is " + x);
    }
    else{
        document.write("Output: the two numbers are: and the largest number is " + y);
    }
}
argestNumber(2,8)